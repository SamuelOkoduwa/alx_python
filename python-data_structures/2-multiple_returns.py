def multiple_returns(sentence):
    length = len(sentence)
    first = sentence[0]
    if sentence == ():
        first == None
    else:
        return ("Length: {:d} - First character: {}" .format(length, first))